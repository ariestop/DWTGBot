-r base.txt

# --- Testing ---
pytest==8.3.3
pytest-asyncio==0.24.0
pytest-cov==5.0.0

# --- Lint / format / types ---
# ruff: pinned to a 0.15.x line so the rule set matches what we validate
# locally (e.g. ASYNC240 only exists from 0.7+; older pins refused our
# pyproject.toml). Bump deliberately to keep CI and developer parity.
ruff==0.15.11
mypy==1.11.2
pre-commit==3.8.0

# --- Stub packages ---
types-redis==4.6.0.20240903
