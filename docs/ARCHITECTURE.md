# Moved

This file is a redirect. The canonical architecture documentation lives in
the numbered `docs/` series:

- [`docs/02-architecture.md`](02-architecture.md) — layers, containers,
  sequence and data-flow diagrams (canonical)
- [`docs/03-project-structure.md`](03-project-structure.md) — folder map
  and dependency rules
- [`docs/04-domain-model.md`](04-domain-model.md) — entities, invariants,
  state machines
- [`docs/07-provider-architecture.md`](07-provider-architecture.md) —
  provider contract
- [`docs/30-add-new-provider-guide.md`](30-add-new-provider-guide.md) —
  step-by-step "extending — adding a new platform" (replaces the old
  inline checklist)
- [`docs/adr/0005-locked-architectural-assumptions.md`](adr/0005-locked-architectural-assumptions.md)
  — locked assumptions register

Start the full reading order from [`docs/README.md`](README.md).

> **Why the redirect?** The old top-level `docs/ARCHITECTURE.md`,
> `docs/DEPLOY.md`, and `docs/TROUBLESHOOTING.md` predate the numbered
> series. They are kept only as pointers so existing external links don't
> break. Do not extend them — extend the numbered docs instead.
