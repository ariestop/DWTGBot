[pytest]
addopts = -ra -q --strict-markers
testpaths = app/tests
asyncio_mode = auto
filterwarnings =
    ignore::DeprecationWarning
markers =
    integration: requires external services
    slow: long running
